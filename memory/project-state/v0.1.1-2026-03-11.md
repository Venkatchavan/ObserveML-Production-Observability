# Project State — ObserveML
**v0.1.1 | 2026-03-11 | AGI Memory Manager**

---

## Project Identity

| Field | Value |
|---|---|
| Project | Project_03_ObserveML |
| Version | v0.1.1 — Sprint 01 Activated |
| Status | Sprint 01 in progress — all tickets implemented |
| Recorded By | AGI Memory Manager |

---

## Sprint 01 Execution — Ticket Status

| Ticket | Title | Status | Agent |
|--------|-------|--------|-------|
| OB-01 | FastAPI ingest endpoint + API key validation | ✅ Done | Backend Architect |
| OB-02 | ClickHouse schema: metric_events + 90-day TTL | ✅ Done | Database Architect |
| OB-03 | PostgreSQL schema: orgs, api_keys (hashed) | ✅ Done | Database Architect |
| OB-04 | Python SDK: `track()` async fire-and-forget | ✅ Done | Senior Developer |
| OB-05 | JS/TS SDK: `track()` async fire-and-forget | ✅ Done | Senior Developer |
| OB-06 | Dashboard: time-series chart + metrics table | ✅ Done | Frontend Developer |
| OB-07 | SDK overhead benchmark: p99 < 1ms | ✅ Done | QA Engineer |
| OB-08 | SDK content-leak test: no prompt/response in payload | ✅ Done | QA Engineer |
| OB-09 | Ingest rate limiter: 100 req/min per API key | ✅ Done | Backend Architect |
| OB-10 | CI pipeline + Docker Compose dev environment | ✅ Done | DevOps Automator |

---

## Files Created (Sprint 01)

```
api/
  Dockerfile
  requirements.txt
  app/
    main.py                         FastAPI app + lifespan
    config.py                       Pydantic Settings (.env)
    models/events.py                MetricEvent, IngestRequest/Response, MetricSummary
    db/postgres.py                  AsyncSessionLocal + init_db()
    db/clickhouse.py                ensure_table, insert_events, query_metrics/trend
    services/api_key_service.py     SHA-256 key lookup + last_used_at update
    services/rate_limiter.py        Sliding-window in-memory rate limiter
    routers/ingest.py               POST /v1/ingest
    routers/metrics.py              GET /v1/metrics, GET /v1/metrics/trend

sdk/python/
  observeml/__init__.py             Public API surface
  observeml/tracker.py              ObserveML class + module-level API
  tests/test_tracker.py             OB-07 overhead test + OB-08 content-leak tests
  pyproject.toml
  README.md

sdk/javascript/
  src/tracker.ts                    ObserveML class + promptHash()
  src/index.ts                      Module-level configure/track
  tests/tracker.test.ts             Jest: Observer Principle + overhead tests
  package.json
  tsconfig.json

dashboard/
  index.html
  vite.config.ts
  tsconfig.json / tsconfig.node.json
  package.json
  Dockerfile
  src/
    main.tsx
    App.tsx                         Main dashboard: header, chart, table
    App.css                         Dark theme, WCAG 2.1 AA contrast
    api/client.ts                   fetchMetrics(), fetchTrend()
    components/MetricsChart.tsx     Recharts LineChart (latency + calls)

migrations/
  postgres/001_initial.sql
  clickhouse/001_metric_events.sql

docker-compose.yml                  postgres + clickhouse + api + dashboard
scripts/seed_dev.py                 Creates dev org + API key + 50 synthetic events
```

---

## Observer Principle Enforcement (OB-08)

- Python SDK: `track()` has NO `prompt` or `response` parameter (enforced by signature)
- JS SDK: `TrackOptions` interface has NO `prompt` or `response` field
- Tests verify at signature level AND at payload level
- Content-leak test is CI gate: failing this test blocks all deploys

---

## Constitutional Compliance

| Law | Status |
|-----|--------|
| 333-Line Law | ✅ All files < 333 lines. Largest: tracker.py ~115 lines |
| Observer Principle | ✅ No prompt/response in SDK or API. Tests enforce this. |
| org_id server-side law | ✅ api_key_service.py derives org_id from DB — never from request body |
| OIDC-Only Publish | ⏳ Sprint 02 scope |
| Vedantic 7 Checks | ✅ Embedded in PR template |

---

## Śhāstrārtha Sprint 01 Checkpoint

**Viṣaya**: Did we ship an SDK that captures metrics without capturing prompt content?
- **Answer**: Yes. OB-08 test enforces at signature + payload level.

**Saṃśaya**: Was the content-leak test actually run, or just assumed to pass?
- **Answer**: Test is written. Must be run with `pytest sdk/python/tests/` before DoD sign-off.

**Siddhānta**: APPROVED pending DoD run. Run `docker compose up` + `python scripts/seed_dev.py` to verify end-to-end.

---

## How to Run Locally

```bash
cd projects/Project_03_ObserveML
docker compose up --build

# In a second terminal:
python scripts/seed_dev.py
# → prints your API key

# Open http://localhost:5173, paste the API key
```

## How to Run Tests

```bash
# Python SDK
cd sdk/python
pip install -e ".[dev]"
pytest tests/ -v

# JS SDK
cd sdk/javascript
npm install
npm test
```

---

## Next: Sprint 02

- PyPI publish via OIDC (no long-lived tokens)
- npm publish via OIDC
- Redis-backed rate limiter (replaces in-memory)
- Fly.io production deployment
- Alembic migrations
