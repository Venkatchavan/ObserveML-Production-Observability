# v0.2.0 — 2026-03-11 — Sprint 02 Complete

## Summary
Sprint 02 (33 points) executed in full. SDKs are now configurable, anomaly detection
is live, the dashboard has an Alerts tab, and CI/CD publish pipelines are wired.

## Tickets Completed (10/10)

| ID | Title | Status |
|----|-------|--------|
| OB-11 | Anomaly detection: sliding window threshold comparison | ✅ |
| OB-12 | Alert model: per-metric thresholds + notification dispatch | ✅ |
| OB-13 | Dashboard: alert feed + threshold configuration UI | ✅ |
| OB-14 | PyPI publish pipeline: GitHub Actions OIDC | ✅ |
| OB-15 | npm publish pipeline: GitHub Actions OIDC | ✅ |
| OB-16 | Fly.io deployment: API fly.toml | ✅ |
| OB-17 | SDK streaming: configurable batch interval | ✅ |
| OB-18 | Dashboard drill-down: per-model metric timeline | ✅ |
| OB-19 | Integration test: events → anomaly → alert fired | ✅ |
| OB-20 | SDK documentation: README + docstrings | ✅ |

## New Files

### Backend
- `api/app/services/anomaly_detector.py` — sliding window (10 min), checks all org rules
- `api/app/services/alert_dispatcher.py` — SSRF-safe webhook dispatch + DB persistence
- `api/app/routers/alerts.py` — CRUD + feed: POST/GET /v1/alerts, GET /v1/alerts/feed
- `migrations/postgres/002_alert_fired.sql` — alert_fired table

### Dashboard
- `dashboard/src/components/AlertFeed.tsx` — real-time fired alert list
- `dashboard/src/components/ThresholdConfig.tsx` — create/delete alert rules
- `dashboard/src/components/ModelDrillDown.tsx` — per-call-site 7-day timeline

### CI/CD + Infra
- `.github/workflows/publish-pypi.yml` — OIDC trusted publisher (no long-lived tokens)
- `.github/workflows/publish-npm.yml` — npm OIDC provenance
- `fly.toml` — Fly.io API deployment config

### Tests
- `api/tests/test_integration.py` — 5 tests covering full event→alert flow

## Modified Files

| File | Change |
|------|--------|
| `api/app/db/clickhouse.py` | Added `query_window_stats()` |
| `api/app/db/postgres.py` | Added `alert_fired` table to `init_db()` |
| `api/app/models/events.py` | Added `AlertRuleCreate`, `AlertRuleResponse`, `AlertFeedItem` |
| `api/app/routers/ingest.py` | Background task → `run_anomaly_check(org_id)` after insert |
| `api/app/main.py` | Registered alerts router |
| `dashboard/src/api/client.ts` | Added fetch/create/delete alert rule + alert feed |
| `dashboard/src/App.tsx` | Two-tab layout: Overview + Alerts; drill-down button in table |
| `dashboard/src/App.css` | Tab nav, alert row, threshold form, drill-down link styles |
| `sdk/python/observeml/tracker.py` | `flush_interval_s` param on `__init__` and `configure()` |
| `sdk/javascript/src/tracker.ts` | `flushIntervalMs` param on constructor |
| `sdk/python/README.md` | Full rewrite with flush config, privacy 3-level guarantee, alert API |

## Constitutional Checks

- **333-Line Law:** All files < 200 lines. Largest: `test_integration.py` ~210 lines — within limit.
- **Observer Principle:** No new prompt/response fields added anywhere.
- **org_id isolation:** All new alert routes use server-side key lookup. Feed filtered by org_id.
- **SSRF:** RFC1918 + loopback blocklist in `alert_dispatcher.py` (`_is_ssrf_safe()`).
- **No hardcoded secrets:** Fly.io secrets injected at runtime; OIDC used for PyPI/npm.

## Next Sprint: Sprint 03

DoD preview from NEXUS plan (Wave 3 — Dashboard + Drift Detection):
- Drift detection: statistical comparison across time windows
- Multi-period alert history (7d / 30d graphs)
- Org management UI (create keys, revoke)
- Redis-backed rate limiter (replace in-memory)
- Mobile-responsive dashboard pass

## Śhāstrārtha Checkpoint

**Viṣaya:** Sprint 02 adds alerting, configurable SDKs, CI/CD publish, and Fly.io deployment.

**Saṃśaya:**
1. DNS rebinding risk on webhook URLs (hostname accepted without DNS resolution)
2. In-memory rate limiter still not Redis-backed (planned Sprint 03)
3. `alert_fired` rows grow unbounded — no TTL or pruning yet

**Siddhāta:** APPROVED with Sprint 03 follow-ups on items 1–3.
